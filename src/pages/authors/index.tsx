import Layout from '@/layouts/_layout';

const AuthorsPage = () => {
  return <>temp</>;
};

AuthorsPage.getLayout = function getLayout(page: any) {
  return <Layout>{page}</Layout>;
};

export default AuthorsPage;
